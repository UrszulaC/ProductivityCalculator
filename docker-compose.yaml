version: '3.8'

services:
  app:
    build: .
    environment:
      - HOST=mysql
      - USER=ula
      - PASSWORD=Gordito10?
      - DATABASE=ProductivityCalculator
    depends_on:
      - mysql
    networks:
      - app-network

  mysql:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: gordito
      MYSQL_DATABASE: ProductivityCalculator
    ports:
      - "3306:3306"
    